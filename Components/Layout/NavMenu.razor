@rendermode InteractiveServer

<div class="navmenu">
    <input type="checkbox" title="Menu expand/collapse toggle" id="navmenu-toggle" class="navmenu-icon" />
    <label for="navmenu-toggle" class="navmenu-icon"><FluentIcon Value="@(new Icons.Regular.Size20.Navigation())" Color="Color.Fill" /></label>
    <nav class="sitenav" aria-labelledby="main-menu">
        <FluentNavMenu Id="main-menu" Collapsible="true" Width="250" Title="Navigation menu" @bind-Expanded="expanded" CustomToggle="true">
            <FluentNavLink Href="/" Match="NavLinkMatch.All" Icon="@(new Icons.Regular.Size20.Home())" IconColor="Color.Accent">Home</FluentNavLink>
            <FluentNavLink Href="message" Icon="@(new Icons.Regular.Size20.Mail())" IconColor="Color.Accent">Message</FluentNavLink>
            <FluentNavLink Target="demo-page" Href="Integration" Icon="@(new Icons.Regular.Size20.PlugDisconnected())" IconColor="Color.Accent">Integration</FluentNavLink>
            <FluentNavLink Target="demo-page" Href="Filter" Icon="@(new Icons.Regular.Size20.Search())" IconColor="Color.Accent">Filter</FluentNavLink>
            <FluentNavLink Target="demo-page" Href="Deduplication" Icon="@(new Icons.Regular.Size20.StackOff())" IconColor="Color.Accent">Deduplication</FluentNavLink>
            <FluentNavLink Target="demo-page" Href="JoinEvents" Icon="@(new Icons.Regular.Size20.Connected())" IconColor="Color.Accent">Join Events</FluentNavLink>
        </FluentNavMenu>
    </nav>
</div>

@code {
    private bool expanded = true;
}

